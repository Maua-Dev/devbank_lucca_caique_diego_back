{
  "name": "devbank-back",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "start": "python -m uvicorn src.app.main:app --reload --host 127.0.0.1 --port 8000",
    "test": "pytest -q",
    "test:single": "pytest -q tests/app/test_main.py::Test_Main::test_get_history"
  }
}
